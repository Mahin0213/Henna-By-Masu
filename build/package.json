{
  "name": "henna-by-masu-build",
  "private": true,
  "version": "1.0.0",
  "description": "Pre-render pipeline: runs the site's React components through server-side rendering and bakes the output into site/*.html. Not part of the deployed site — do not upload this folder to Hostinger.",
  "scripts": {
    "build": "node build.js",
    "package": "node build.js && node package.js"
  },
  "dependencies": {
    "@babel/core": "^7.28.5",
    "@babel/preset-react": "^7.27.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
